{{ ansible_managed | comment }}

[Unit]
Description=Registry ({{ registry_name }})
After=network.target

[Service]
Type=simple
ExecStart={{ registry_install_dir }}/registry serve {{ registry_config_dir }}/{{ registry_name }}.yml
Restart=always
RestartSec=5s

[Install]
WantedBy=multi-user.target
